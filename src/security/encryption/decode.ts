const bcrypt = require('bcryptjs');

//tem que rever o user data, user password e fazer uma configuração, tem que alterar o bcrypt para ts.
/*export default async function decode(password: string): string{


  const passwordsMatch = await bcrypt.compare(inputPassword, hashedPassword);
  return passwordsMatch;

}*/

